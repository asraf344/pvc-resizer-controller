---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: pvcresizes.volume.makro.com
spec:
  group: volume.makro.com
  names:
    kind: PVCResize
    listKind: PVCResizeList
    plural: pvcresizes
    singular: pvcresize
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PVCResize is the Schema for the pvcresizes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PVCResizeSpec defines the desired state of PVCResize
            properties:
              policies:
                description: Foo is an example field of PVCResize. Edit pvcresize_types.go
                  to remove/update
                items:
                  properties:
                    ref:
                      description: Name of the PVC
                      type: string
                    size:
                      description: Size of the pvc
                      type: string
                  required:
                  - ref
                  - size
                  type: object
                type: array
            type: object
          status:
            description: PVCResizeStatus defines the observed state of PVCResize
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
